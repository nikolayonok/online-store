package com.onlinestore.repository;

import com.onlinestore.entity.BuyStep;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BuySteepRepository extends JpaRepository<BuyStep, Long> {
}
